#include "tracer.h"
#include "util.h"

bool http2POST(const std::string& ip, const std::string& api, const std::string& json) 
{
    return true;
}

int main(int argc, char *argv[])
{
    curl_global_init(CURL_GLOBAL_ALL);
    
    return 0;
}